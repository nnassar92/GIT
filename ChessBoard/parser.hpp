#include <iostream>
#include <sstream>
#include <string>
#include <stdlib.h>
#include <fstream>
#include <stdio.h>

using namespace std;
namespace parser
{
	int inputint(string,int,bool);								//Makes sure the input is an integer that it returns
};
